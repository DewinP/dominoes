import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const ScoreKeeperScreen = () => {
  return (
    <SafeAreaView>
      <View className="flex flex-col min-h-full px-6 py-12 bg-gray-900 ">
        <Text className="text-white">Lol</Text>
      </View>
    </SafeAreaView>
  );
};

export default ScoreKeeperScreen;
